{
  "name": "sams-root",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "server",
    "client",
    "simulator"
  ],
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}